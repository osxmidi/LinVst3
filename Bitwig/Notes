LinVst3 may have some problems with some plugins and Bitwig.

Use the Makefiles with Bitwig in their name, Makefile-Bitwig.

Choose (check) the plugins to be run Individually in the Bitwig Settings-Plug-ins panel.
